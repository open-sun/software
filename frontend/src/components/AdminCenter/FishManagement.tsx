// src/components/admincenter/FishManagement.tsx
import React from "react";
import { Typography } from "@mui/material";

const FishManagement: React.FC = () => {
  return (
    <Typography variant="body1">
      此处为鱼类管理功能的内容展示区域。
    </Typography>
  );
};

export default FishManagement;
