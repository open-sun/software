// src/components/admincenter/HydrologyManagement.tsx
import React from "react";
import { Typography } from "@mui/material";

const HydrologyManagement: React.FC = () => {
  return (
    <Typography variant="body1">
      此处为水文管理功能的内容展示区域。
    </Typography>
  );
};

export default HydrologyManagement;
